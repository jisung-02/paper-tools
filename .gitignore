# Built WebAssembly — produced by build.sh, rebuilt in CI on deploy
web/*/*.wasm
.staging/
web/wasm_exec.js
web/operation-catalog.json

# Stray local build output (go build without -o drops binaries in the repo root)
/hwp2pdf
/pdfimages
/pdftext

# OS / editor cruft
.DS_Store
*.log

# Local tool state / caches
.gstack/
.wrangler/
.worktrees/
node_modules/
test-results/
playwright-report/
web/.gstack/
/web/ko/
/web/ja/
/web/zh/
/web/es/
/web/fr/
/web/de/
